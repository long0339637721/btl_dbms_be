
export class ProductEntity {
  id; // Fix: Declare id as a variable
  code = ""; // Fix: Initialize code as an empty string
  name = ""; // Fix: Initialize name as an empty string
  color = ""; // Fix: Initialize color as an empty string
  sale_percent = 0; // Fix: Initialize sale_percent as 0
  price = 0; // Fix: Initialize price as 0
  manufacturer = ""; // Fix: Initialize manufacturer as an empty string
  html = ""; // Fix: Initialize html as an empty string
  image = ""; // Fix: Initialize image as an empty string
  category_id = 0; // Fix: Initialize category_id as 0
}
